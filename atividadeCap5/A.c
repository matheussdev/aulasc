#include <stdio.h>

int main(){
    // define variáveis dos valores

    int i, q; //variável de controle do loop e variavel de operação

    //operar valores

    for ( i = 15; i <= 200; i++)
    {   
        q = i*i;
        printf("\n %dx%d = %d", i, i, q);
    }
        

    

};
